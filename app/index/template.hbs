    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-3 col-md-2 sidebar">
           {{side-bar}}
        </div>
        <!-- <div class="jumbotron"> -->
          <div class="container">
          <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
              <ul>
                {{#each model.products.data as |product| }}
                {{#link-to 'product'}}<li>{{product.attributes.name}}</li>{{/link-to}}
                {{/each}}
              </ul>


              <!-- <li><b>Name:</b> {{product.attributes.name}}</li>
              <li><b>Image:</b>{{product.attributes.image}}</li>
              <li><b>Barcode:</b>{{product.attributes.barcode}}</li>
              <li><b>Price:</b> {{product.attributes.price}}</li>
              <br>
              {{/each}} -->
          </div>
        </div>
      </div>
  </div>



    <!-- <h1>Accounts</h1>
    <ul>
      <li><strong>Accounts</strong></li>
      <ul>
        {{#each model.accounts.data as |account| }}
        {{#link-to 'product'}}<li>{{product.attributes.name}}</li>{{/link-to}}
        {{/each}}
      </ul>
    </ul> -->
